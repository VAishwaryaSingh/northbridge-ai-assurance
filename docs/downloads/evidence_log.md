# Evidence Log

Subject: Northbridge Ledger Agent, pinned commit `9c4127ad65aa5a9e7d362c2d05f69ecfdbf7c5f9`.
Entries are written as testing is performed (contemporaneous). Times are UTC. Ledger of what was tested, how, sample, result and evidence reference.

## E-01 Test dataset and labels frozen before first run
- **Time:** 2026-09-23 17:33Z
- **What:** synthetic test ledger (same schema as Northbridge) and ground-truth labels generated by `test_scripts/build_dataset.py` (seed 2026) against the pinned subject. Labels reflect accounting meaning (is the record really wrong?), including legitimate "hard" items labelled clean.
- **Frozen hashes (SHA-256):**
  - `test_dataset/ledger_test.db` `a1270db5f7d33f535d97bc1c42578bfbbcd2c87458c16207ee2fbd2423e08781`
  - `test_dataset/labels.csv` `d6ea3f10879170009f86411d165a904aaede41bc172e945f7d0075786bab3779`
  - `test_scripts/build_dataset.py` `3f7f09e43b1cf2ea5e02df29fe0d3be946bc565b39d64091d4d425610e49877c`
  - `test_scripts/run_evaluation.py` `abbf3ba32fc8e7d20d30dcd31a9512953c57b5a6bc19fe15d967bf6ad4af23dc`
- **Composition:** 230 items (bills, sales invoices, bank lines): 31 real errors (8 types), 199 clean (167 standard, 32 hard cases). Clean share 86.5%, above the 50% minimum.
- **Deviations and limits, recorded before testing:**
  1. 230 items exceeds the master plan's 100 to 200 range; kept because more clean items tighten the false positive estimate.
  2. **Independence of the test set is limited.** The reviewer wrote both the system and this dataset, and had read the rules before designing cases. Mitigations: labels follow accounting meaning rather than rule logic, hard legitimate cases are included, labels frozen before running, the challenge review (4.4) can re-examine them.
  3. Error counts per type are small (3 to 6), so per-type recall carries wide uncertainty (Wilson 95% intervals reported).
  4. "Missing documentation" (in the original plan) is not tested: Northbridge has no such function.
  5. Weekend-dated bills: the record does not show whether a supplier trades at weekends, so legitimate weekend items are labelled clean; A4 false positives there are inherent to the rule design, not a bug.
- **Subject setup:** `test_scripts/setup_subject.sh` clones the original repo to `04_testing/_subject` and checks out the pinned commit (verified `9c4127a...`). The original working tree is not touched.

## E-02 Evaluation harness run over the frozen test set
- **Time:** 2026-09-23 17:34Z. **How:** `test_scripts/run_evaluation.py` using the subject's own venv (Python 3.11), read-only DB connection, engine entry points `match_bank_transactions`, `build_reconciliation_report`, `detect_all` at the pinned commit. Dataset hashes as E-01.
- **Sample:** 230 items, 31 errors, 199 clean (167 standard, 32 hard).
- **Result (item level, any rule):** TP 28, FN 3, FP 22, TN 177. Recall 0.903 (95% CI 0.751 to 0.967), precision 0.560 (0.423 to 0.688), F1 0.691, false positive rate 0.111 (0.074 to 0.162). FPR on standard clean items 2/167 = 0.012; on hard clean items 20/32 = 0.625.
- **By error type (recall, any rule):** duplicate payment 4/4, reconciliation break 6/6, duplicate sales invoice 5/5, VAT miscoding 4/4, threshold 3/3, weekend 3/3, outlier 3/3, **duplicate supplier bill 0/3**.
- **Criteria (ToR s6):** recall on duplicates and breaks **not met** (15/18 = 0.833 against 0.95, driven entirely by duplicate supplier bills); recall on other rules met (13/13); precision **not met** (0.56); F1 **not met** (0.691); false positive rate **not met** (0.111 against 0.10).
- **Evidence:** `results/metrics_overall.csv`, `metrics_by_error_type.csv`, `metrics_by_rule.csv`, `criteria_assessment.csv`, `item_results.csv`, `detections.csv`, `run_metadata.json`, `recall_by_error_type.svg`.
- **Interpretation notes:**
  1. **Duplicate supplier bills are not detected at all** (AP-121, AP-122, AP-123): rule A1 checks sales invoices only. Confirms R04.
  2. **22 false positives break down as:** 6 monthly bank charges (no bill exists; A6), 4 legitimate weekend supplier bills (A4), 3 bills from a supplier newly VAT-registered (A2), 5 legitimate fixed-price or boundary bills flagged by the outlier check plus A3 (AP-093 to AP-097; these sit at about 3x the account norm, so flagging is arguable), 2 legitimate large equipment purchases (A7), and **2 clean customer receipts flagged as duplicate payments (BT-023, BT-035)**. Most hard-case false positives are by design of the test. The last two are not: see 3.
  3. **Matcher weakness (new):** for recurring invoices of identical amount to one customer, the matcher takes the first invoice within the date window, even if it is already paid, so a legitimate later receipt is attributed to an earlier paid invoice and A5 reports a duplicate payment. Verified from the database for BT-023 (attributed to AR-001, intended AR-002) and BT-035. Retainer billing is common, so this is likely on real data.
  4. Recall on the seven detectable types is high, but the test only contains error types Northbridge is designed to detect, and the reviewer designed the cases after reading the rules (limit 2 in E-01). It shows the rules work on constructed cases and where they fail, not that recall on real errors will be high.
  5. Per-type samples are 3 to 6; confidence intervals are wide (e.g. 3/3 has a lower bound of 0.44).

## E-03 Determinism
- **Time:** 2026-09-23 17:34Z. **How:** full engine run in 5 fresh Python processes (hash randomisation on) over the unchanged test database, SHA-256 of sorted detections compared; also an in-process run.
- **Result:** 5 of 5 identical (`36309d14...01cf2`); in-process run identical. 100% against a 100% criterion: **met**. IsolationForest path (not the default): 3 of 3 identical across fresh processes (`control_tests_output.txt`, C14).
- **Limit:** same environment, same dependency versions. Dependencies are unpinned, so identity across installs is not shown.
- **Evidence:** `results/run_metadata.json` (determinism block), `results/control_tests_output.txt`.
- **Runtime:** engine 0.009 s on 230 items (5,000-row synthetic view reported at about 0.5 s by the project).

## E-04 Control operating tests (from pinned code and committed data)
- **Time:** 2026-09-23 17:35Z. **How:** `test_scripts/control_tests.py`; full output in `results/control_tests_output.txt`. Secrets were not printed (counts and filenames only). The local `.env` in the original working tree was not opened.
- **Results by control** are recorded in `03_controls/control_matrix.xlsx` (Operating result and Evidence ref columns). Highlights:
  - C01, C02: no write scopes for Xero; QuickBooks code issues only GET and token POST, but the QuickBooks scope granted is `com.intuit.quickbooks.accounting`.
  - C04, C06: `.env` ignored, zero commits ever touched it; `.env.example` holds only two non-secret redirect URIs; secrets scan of all history found no credentials (one regex hit was a comment divider line in `seed_synthetic.py`, a false positive). The scan was a one-off by the reviewer, not a repeatable control owned by the project.
  - C05: refresh tokens written with `set_key` to `.env` (plaintext), Xero and QuickBooks.
  - C11: dashboard data-quality counts equal an independent recount for all 3 committed databases.
  - C16: published figures reproduce exactly (synthetic 84/84, 24 FP, precision 0.778; Xero 7/7, 3 FP, precision 0.70). Reproducible, but circular.
  - C19: on the test data every unmatched bank line (12 of 12) appears in the unmatched list.
  - C20: 15 of 15 sentence headline figures agree with source rows. **But** see E-05.
  - C22: all 54 flags on the test data carry a reason naming the contact.
  - C13, C15, C21, C24, C25, C26, C28: confirmed absent (no tests directory, 0 tags, no lockfile or pins, no logging, hashing, run id, disposition, approval or version code; schema has 6 data tables and no audit table).
  - C09, C27: README does not state intended use, limitations on real books, false negatives or production status; "rule-based checks" appear only in the accuracy section (line 108).
  - C07: committed contacts are fictional, seeded ("Customer 01") or Intuit sandbox names. The Xero demo uses real brand names as counterparties (for example Amazon Business, BT Business, NatWest); no personal data seen. Observation only.
  - QuickBooks 0 of 40 auto-matched: 40 bank lines, only 3 distinct bank contacts also appear on any invoice or bill, 35 are SPEND lines (purchases) with vendor IDs and 7 have no contact. **Probable cause (not proven):** sandbox purchases are direct expenses that have no bill, so the "bank line must match a bill" model does not fit; every such line is reported as unmatched, adding noise.
- **Not tested:** C12 (pagination against live APIs: credentials and the Xero trial are not available to this review; mock tests only), Xero and QuickBooks granted scopes in the vendor portals, C29 (challenge review pending).

## E-05 Variance explanation direction check (new defect)
- **Time:** 2026-09-23 17:37Z. **How:** `test_scripts/variance_direction_check.py`: for each account and consecutive month pair, compare the headline direction ("up"/"down") with the sign of each named driver.
- **Result:** test dataset: 17 of 52 sentences (33%) name a driver that moved the opposite way to the headline (e.g. "Consulting Fees down 56% ... driven by Fairway Foods (+£1,114)"). Northbridge's own data: synthetic 35 of 69, QuickBooks 4 of 14, Xero snapshot 0 of 5.
- **Cause (from code, `explainer.py:88-99`):** the top two contacts by absolute change are named "driven by" regardless of sign, so the contacts named need not explain the headline movement.
- **Effect:** the sentence reads as an explanation but can point at a movement that offset, not caused, the change. No hallucination is involved; the text is templated, which makes it a design defect.
- **Evidence:** `results/variance_direction_check.txt`.

## E-06 Independent challenge review: outcome and corrections to earlier entries
- **Time:** 2026-09-23 (after E-05). Full disposition table in `challenge_review.md`; raw report in `challenge_review_raw.md`. Model-based review by a separate Claude session.
- **New evidence:** `results/order_sensitivity.txt` (script `test_scripts/order_sensitivity.py`), `results/sensitivity.csv` and `results/criteria_assessment_full.csv` (script `test_scripts/sensitivity_and_full_criteria.py`), `results/git_state.txt`, `results/sensitivity_output.txt`.
- **Order sensitivity (CH-01, verified):** the same data loaded in a different row order changes the output. Original order 54 flags, 28 error items detected; reversed order 53 flags and duplicate payment BT-044 lost; 5 of 6 random shuffles also differed. Bank-line false positives move between BT-023/BT-035 and BT-026/BT-031. The matcher takes the first acceptable match with no ordering and no one-to-one allocation.
- **Corrections (these supersede the earlier wording):**
  - **E-01:** "labels follow accounting meaning rather than rule logic" holds for duplicates, breaks and VAT, but not for weekend, threshold and outlier types, whose labels are judgement (see `sensitivity.csv` S1). "Frozen before the first run" is self-attested (hashes recorded at 17:33Z, same minute as the run).
  - **E-02 note 2:** "most hard-case false positives are by design" is circular: the reviewer chose those cases. **Note 3:** "likely on real data" is unsupported; demonstrated on constructed data only, and the cause is wider than recurring invoices (first-match, no one-to-one allocation, order dependence). Per-flag precision is 0.537 (29/54); 0.560 is per item.
  - **E-03:** determinism "met" is **withdrawn**: identical only for one fixed row order; **not met** when row order changes (6 of 7 orderings differ). The IsolationForest identical-run result carries the same limit.
  - **E-04:** C22 "54/54 name the contact" is unsupported (A6 excluded from the test; a null contact passes; BT-050 reads "Bank line for None"). C19 "12/12 listed" only shows the list equals its own source; QuickBooks 0/40 arises from sandbox data (no same-contact, same-amount invoice for any bank line), not shown to be a matcher failure. C06 means "no matches for these patterns in code files (md, csv, db excluded)", not a scanner run. C07 sampled only the first 12 names per database, so "no personal data" is unsupported (the QuickBooks demo has address-like contact names). C11 recounts against queries on the same database, so it shows arithmetic consistency, not pull completeness. C23 evidence is now `results/git_state.txt` (13 commits, 0 tags).
  - **E-05:** the 17 of 52 count is sentences naming at least one driver that moved against the headline (some are mixed). In addition, only contacts present in the later period are ever named (`explainer.py:88-93`), so a contact that disappeared cannot be named.
- **Criteria on the full ToR list:** 7 of 8 not met (see `criteria_assessment_full.csv`).

## E-07 Remediation demonstrated on a copy; retest
- **Time:** 2026-09-23 (after E-06). The Northbridge project was not modified (HEAD `9c4127a`, no modified files, verified after the work). Fixes were made in `04_testing/_subject_remediated` and captured in `05_reporting/remediation.patch`; the patch applies cleanly to a fresh pinned checkout and the 10 unit tests pass there.
- **Retest:** frozen harness and dataset unchanged (hashes verified equal to E-01 before the retest). Results in `04_testing/results_remediated/`; summary and caveats in `05_reporting/remediation.md`.
- **Headline:** recall 0.903 to 0.968; precision 0.560 to 0.600; FPR 0.111 to 0.101; duplicate supplier bills 0/3 to 3/3; output identical across 7 row orderings (was 6 of 7 differing); variance sentences with an opposing driver 17/52 to 0/59.
- **Adverse effects recorded:** BT-044 duplicate payment no longer flagged as A5 (ambiguity with a duplicate bill in the dataset); published synthetic precision 0.778 to 0.712 (10 new A1 supplier-bill flags).
- **Limit:** fixes designed against the same dataset used to retest; not evidence of accuracy on real books.

## E-08 Northbridge README updated after the review
- **Time:** 2026-09-23. At the owner's request a plain statement was added to the Northbridge README working tree (not part of the pinned commit): the tool is rules-based, calls no LLM or AI model, is a demonstration on fictional and sandbox data, is not production software, and every flag needs human review.
- **Effect on this review:** all evidence and findings refer to the pinned commit `9c4127a`, which did not contain the statement. F-10 describes that commit. The added sentence covers part of F-10 (no LLM, not production, human review of flags); intended use and the known limitations (F-01, F-02, F-08) are still not stated in the README.
